using Autofac;

namespace Atlas.AbpModules
{
    // Placeholder module to register ABP services via Autofac.
    public class AtlasAbpModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            // TODO: register ABP services here
        }
    }
}
